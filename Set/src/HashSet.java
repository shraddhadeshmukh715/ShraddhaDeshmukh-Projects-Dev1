/* 
 * HashSet :-
 * 1. It is an class that implements set interface
 * 2. It internally uses HashMap to store the data as objects
 * 3. not allow duplicate elements
 * 4. allows only one null key/element.
 */

import java.util.Set;

public class HashSet {

	Set<Integer> s = new java.util.HashSet<>();
	
	s.add()
	
}
